// firebase-admin.ts — STUBBED for localhost-only operation
// All data operations now go through Drizzle ORM (src/lib/db.ts)
// This file is kept to prevent import errors in any files not yet migrated.

export const db = null as any;
export const auth = null as any;
